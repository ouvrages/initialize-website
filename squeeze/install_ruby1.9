#!/bin/sh

apt-get install -y ruby1.9.1 ruby1.9.1-dev

for i in ruby gem irb; do
  ln -vfs ${i}1.9.1 /usr/bin/$i
done

REALLY_GEM_UPDATE_SYSTEM=true gem update --system
gem install bundler

[ -e ~/.gemrc ] || echo "gem: --bindir /usr/local/bin --no-ri --no-rdoc" >~/.gemrc
